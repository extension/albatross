<% @page_title = "All Deployments" %>
<div class="page-header">
  <h1>All Deployments</h1>
</div>  

<div class="row-fluid">
  <div class="span12">
	  <%= paginate @deploylist %>
	  <%= render(:partial => 'list', :locals => {:deploylist => @deploylist} )%>
  </div>
</div>
